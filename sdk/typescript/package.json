{
  "name": "indexone",
  "version": "0.0.1",
  "description": "IndexOne TypeScript SDK — thin bindings over the Rust core via the indexone-cli sidecar.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test dist/test/*.test.js"
  },
  "license": "TODO",
  "private": true,
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6"
  }
}
